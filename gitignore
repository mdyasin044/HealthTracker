# Local properties (contains API keys - NEVER commit)
local.properties

# Android Studio / IntelliJ IDEA
/.idea
*.iml
.DS_Store

# Gradle
.gradle
/build
gradle/wrapper/gradle-wrapper.jar

# App build outputs
app/build/

# Native build
.externalNativeBuild
.csar

# Keystore files (never commit)
*.jks
*.keystore

# Google services (may contain sensitive info)
google-services.json
